import { Request, Response } from 'express';
import DeviceEntity from '../entities/device.entity';
// import type { CustomRequest } from '../types/global.type';

// export const listOfDevices = async (req: CustomRequest, res: Response) => {
// 	const userId = req.userId;

// 	// const { categories, devices } = await DeviceEntity.getAllDevices(userId);

// 	// return res.json({ devices, categories });
// };
